import 'package:flutter/material.dart';

class CustomArtImage extends StatelessWidget {
  const CustomArtImage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/images/ART.png',
    );
  }
}
